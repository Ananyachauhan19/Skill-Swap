import React, { useEffect, useState } from 'react';
import { BACKEND_URL } from '../config.js';

const SkillMateRequests = () => (
  <div className="space-y-4">
    <h1 className="text-xl font-semibold text-gray-800">SkillMate Requests</h1>
    <div className="p-4 rounded-lg bg-white border border-gray-100 shadow-sm text-sm text-gray-600">
      Update in future: Admin SkillMate requests view will be implemented here.
    </div>
  </div>
);

export default SkillMateRequests;
