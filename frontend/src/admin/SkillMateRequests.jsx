import React, { useEffect, useState } from 'react';
import { BACKEND_URL } from '../config.js';

const SkillMateRequests = () => (
  <div className="p-8">
    <h2 className="text-2xl font-bold mb-4">SkillMate Requests</h2>
    <p className="text-gray-700">Update in future: Admin SkillMate requests view will be implemented here.</p>
  </div>
);

export default SkillMateRequests;
