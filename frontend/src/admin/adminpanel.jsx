// Deprecated legacy panel replaced by routed pages + AdminLayout.
// Keep minimal placeholder to avoid breaking imports until fully removed.
import React from 'react';
const AdminPanel = () => (
  <div className="text-sm text-gray-500">Redirecting...</div>
);
export default AdminPanel;